using PontPesee.API.DTOs;

namespace PontPesee.API.Reports
{
    // Placeholder class. PDF generation now performed inline in controllers
    // using QuestPDF.Document.Create(...) to avoid version/API mismatches.
    public class StatisticsReport
    {
        public StatisticsReport(PeseeStatsDto stats) { }
    }
}
